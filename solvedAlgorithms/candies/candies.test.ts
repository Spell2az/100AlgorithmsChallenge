import { candies } from './candies';

describe('candies', () => {
  it('should output number', () => {
    expect(candies(3, 10)).toBe(3)
  })

  it('reslut shoud be 7', () => {
    expect(candies(3, 22)).toBe(7)
  })
}
  , )

